# rename this file to Golf.yml and put a : in front of "name" if
# you want to use yaml like in old versions of Catalyst
name: Golf
Model::Kioku:
    dsn: "bdb-gin:dir=db/"
#    dsn: "dbi:SQLite:dbname=db.sqlite"
session:
    flash_to_stash: 1